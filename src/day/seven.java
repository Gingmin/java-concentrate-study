package day;

public class seven {

	public static void main(String[] args) {

		12-22
		
		compareTo() 사전 순 비교 같으면 0, 작으면 음수, 크면 양수(앞에서 뒤에를 빼라)
		str2.compareTo(str3)
		
		compartToIgnore()
		
		concat() 문자열 합치기
		str2.concat(str5)
		
		indexOf() 문자열 특정 문자 탐색 - 인덱스 값 반환, 없으면 -1
		
		lastIndexOf() 뒤에서 탐색
		
		trim() 앞 뒤 공백 제거 (원본 영향x)
		
		toLowerCase() 소문자로 변환
		toUpperCase() 대문자로 변환
		원본 영향x
		
		subString() 문자열의 일부를 잘라 새로운 문자열 만듦 (원본 영향x)
		
		replace() 문자열에서 기존문자열을 대체할 문자로 변경 후 반환
		
		length() 길이 반환
		
		isEmpty() 길이가 0이면 true, 아니면 false(0인 거는 null과는 다르다)
		
		
		문자열 객체를 만드는 방법
		리터럴 형태 - ""
		새로운 인스턴스 - new String("")
		
		문자열에 + 연산 - 문자열은 불변이기에 수정이 아닌 새로운 문자열 할당
		
		
	}

}
